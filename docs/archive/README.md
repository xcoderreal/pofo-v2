# Archive

Pre-rebuild artifacts, moved here 2026-08-01 when the portfolio-tracker domain design was crystallized via `/grill` (see `UBIQUITOUS_LANGUAGE.md` at repo root). Kept for reference; not part of the active doc set and not linked from `CLAUDE.md`.

- **`experiments/`** — the skeleton's hypothetical experiment specs (mealwise, pofo v2/v3, auth middleware, etc.). Superseded once real docs/decisions exist for what's actually being built.
- **`notes/`** — cross-repo skeleton-maintenance research (survey notes on sibling forks, in-progress skeleton questions). Not project-specific.
- **`auth.md`**, **`security.md`** — the skeleton's generic/placeholder auth and security docs. Superseded by project-specific versions reflecting the real Supabase Auth + RLS design (also fixes stale `owner_id` → `user_id` terminology drift versus the rest of this repo).
